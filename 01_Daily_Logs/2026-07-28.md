[[Week1_Checklist.md]]
# Daily Log - Tuesday, July 28, 2026

## ⏱️ Work Hours Tracker
- **Start Time:** 08:00
- **End Time:** 17:00
- **Break:** 12:30 - 13:30
- **Hours Today:** 8.0 h
- **Weekly Total:** 16.0 / 40.0 h

---

## 🎯 Today's Tasks
- [x] Implement and test `AutoTokenizer` loading with Hugging Face `transformers` — loaded Qwen2.5-0.5B's tokenizer, inspected real token IDs for a French sentence
- [x] Understand subword tokenization (BPE/WordPiece) and inspect special/control tokens (`BOS`, `EOS`, `PAD`) — wrote a from-scratch BPE merge demo on a toy corpus (matches the "low/newest" example from Sennrich et al. 2016); confirmed special tokens are manually reserved, not learned via merges
- [x] Apply chat templates (`tokenizer.apply_chat_template`) to format conversational data — tested with/without `add_generation_prompt`, discovered Qwen injects a default system prompt when none is given
- [x] Load dataset via `datasets` library and create reproducible `train` (80%), `validation` (10%), and `test` (10%) splits — real dataset (databricks-dolly-15k, 15,011 rows), implemented as `split_dataset()`/`load_split_dataset()` in `src/data.py` with tests
- [x] Map dataset with tokenization function and save preprocessed data to cache/disk — pending; `format_as_chat_messages()` builds the chat messages, but the actual tokenize-and-cache-to-disk step isn't written yet

---

## Concepts Covered Today (In English)
### Tokenization & Chat Templates
- **Subword Tokenization:** Breaks text into subword units, balancing vocabulary size and out-of-vocabulary handling.
- **Control Tokens:** Special markers (e.g., `<|im_start|>`, `<|im_end|>`) defining message boundaries in conversational models.
- **Chat Templates:** Jinja2 templates ensuring text formatting matches the instruction-tuned model's original training structure.
- **Data Splitting:** Creating isolated train, validation, and test splits to prevent data leakage during evaluation.

### Deep-Dive Recap: Tokenizers, BPE, and Real Data

**Tokenizer vs. tokenization** finally clicked as tool vs. process: tokenization is the *action* of cutting text into pieces; the tokenizer is the concrete *object* (vocabulary + merge rules + special tokens) that performs it. Loading Qwen2.5-0.5B's tokenizer and running it on a French sentence showed this isn't abstract — "the" stayed a single token, "Afrique" split into "Af" + "rique", because the tokenizer's ~151k-entry vocabulary emerged from statistics over a training corpus that wasn't French/African-language-heavy.

**BPE, built from scratch on paper's own toy example** (`low`×5, `lower`×2, `newest`×6, `widest`×3): starting from raw characters, the algorithm repeatedly merges whichever *adjacent pair* is most frequent across the whole corpus — `e`+`s` → `es`, then `es`+`t` → `est`, then `l`+`o` → `lo` → `low`, then `n`+`e` → `ne` → `new`. After just 6 merges, `newest` is already down to 2 symbols (`new`+`est`) instead of 6 characters. Real tokenizers just repeat this tens of thousands of times. This is exactly why the vocabulary isn't hand-designed — it's a statistical artifact of whatever corpus trained the tokenizer.

**Special tokens are the exception to that rule:** `<|im_start|>`, `<|im_end|>`, `<|endoftext|>` are manually reserved vocabulary slots, never built by merging, never split apart — that's why they always survive intact regardless of context.

**Chat templates**, tested on a real Dolly-15k example: `apply_chat_template()` wraps each turn in `<|im_start|>{role}` ... `<|im_end|>`, and `add_generation_prompt=True` appends a trailing, empty `<|im_start|>assistant` cue used only at inference time (not needed during SFT, where the assistant's response is already present). Also caught a real bug in my own demo script: newer `transformers` returns a dict (`input_ids` + `attention_mask`) from `apply_chat_template(tokenize=True)` instead of a bare list — `len()` on that dict silently counted 2 keys instead of tokens. Fixed by indexing `['input_ids']`.

**Dataset splitting, on real data this time:** loaded databricks-dolly-15k (15,011 rows, CC BY-SA 3.0), split reproducibly into 12,008/1,501/1,502. Found 24 instructions shared between train and test (e.g. "Why are plants green?" asked more than once with different valid answers) — not literal duplicate rows, but a real, mild form of contamination exactly matching what the internship contract (Week 5) asks to investigate. The fix (dedup by instruction before splitting) is deferred to when a real research dataset is chosen.

**`src/data.py` went from stub to real code:** `split_dataset()` (pure logic, tested in 1.84s with a 100-row in-memory fake dataset, no network) and `load_split_dataset()` (thin, network-touching wrapper, verified manually against the real config) are now separate on purpose — fast/reliable unit tests for logic, manual verification for the network-dependent part.

---

## Daily English Practice
- **Podcast listened:** *Practical AI* (15 min)
- **Speaking exercise:** Spoke for 15 minutes out loud explaining the differences between Base models, Instruction models, and Chat templates.
- **New Vocab:** *Truncation, Padding, Data leakage, Subword, Special tokens*.

---